#include <iostream>

int main(){

  std::string text = "Hello World!";

  // text.length() - outputs length of the text
  // text.empty() - check if a string is empty
  // text.append() - appends smth to the end
  // text.at(0) - return a character at index 0 of the string
  // text.insert (0, @) - inserts @ at the index 0 of the string pushing aside the rest.
  // text.find("a") - return index of 'a' in the string
  // text.erase(0, 2) - removes characters from index 0 to index 2

  return 0;
}

